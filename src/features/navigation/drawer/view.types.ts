/**
 * PanelUI semantic tokens resolved once per drawer render and passed into the
 * platform-native trees, following the composer's `ComposerColors` pattern.
 */
export interface DrawerColors {
  background: string;
  card: string;
  destructive: string;
  foreground: string;
  muted: string;
  mutedForeground: string;
  primary: string;
}
